# notebooks/

Chỉ dùng để khám phá (EDA, thử ý tưởng). Không có con số nào trong notebook được coi là kết quả chính thức: kết quả chính thức chỉ đến từ `python -m credit_risk run`.

Quy ước đặt tên: `NN_<chủ đề>.ipynb`, ví dụ `01_home_credit_eda.ipynb`. Xoá output trước khi commit.
